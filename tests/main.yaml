---
# vars file for Dell-Networking.dellos-bfd, below gives a sample configuration

# Sample variables for dellos10 device
dellos_bfd:
  bfd:
    interval: 100
    min_rx: 100
    multiplier: 3
    role: "active"
    state: "present"
